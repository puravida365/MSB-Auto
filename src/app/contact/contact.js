(function(module) {
    
    module.controller('ContactController', function () {
        var model = this;

        init();

        function init() {

        }
    });
    
}(angular.module("MSBTire.contact")));