(function(module) {
    
    module.controller('ServicesController', function () {
        var model = this;

        init();

        function init() {

        }
    });
    
}(angular.module("MSBTire.services")));